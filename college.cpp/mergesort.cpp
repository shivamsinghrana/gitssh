#include <bits/stdc++.h>
using namespace std;
void mergesort (arr[]){
    if(arr=null||arr<=1){
        return;
    }
    temp arr=arr;
    }
    int n1=left-right+1;
    int n2=left -right;
    int m[l1],
    for (int i=0;i<n1;i++)
    i=left;
    j=mid+1;
    k=left;

}

